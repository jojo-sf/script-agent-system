# Script Agent System — Python Dependencies
# All tools use only the Python standard library (urllib, json, argparse)
# No additional packages required for core functionality

# Optional: for loading .env files
python-dotenv>=1.0.0
